name: Build, Sign and Release APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  APK_NAME: OTA-Deneyi.apk
  RELEASE_TAG: OTA-Deneyi-Realses

jobs:
  build-sign-release:
    name: Build, Sign & Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout kodu
        uses: actions/checkout@v4

      - name: JDK 17 kur
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle

      - name: Gradle izinlerini ayarla
        run: chmod +x ./gradlew

      - name: Debug APK derle
        run: ./gradlew assembleDebug --stacktrace --no-daemon

      - name: Sürüm kodunu oku
        id: version
        run: |
          CODE=$(grep -oP 'versionCode\s+\K[0-9]+' app/build.gradle)
          echo "code=$CODE" >> "$GITHUB_OUTPUT"

      - name: APK'yı imzala
        env:
          PRIVATE_KEY: ${{ secrets._PRIVATEKEY_ }}
        run: |
          set -euo pipefail

          KEY_FILE="$RUNNER_TEMP/private_key.pem"
          printf '%s\n' "$PRIVATE_KEY" > "$KEY_FILE"
          trap 'rm -f "$KEY_FILE"' EXIT

          SRC_APK=$(find app/build/outputs/apk/debug -name '*.apk' | head -n1)
          test -f "$SRC_APK"

          cp "$SRC_APK" "$APK_NAME"

          openssl dgst -sha256 -sign "$KEY_FILE" -out signature.sig "$APK_NAME"
          base64 -w0 signature.sig > signature.b64
          rm -f signature.sig

      - name: update.json oluştur
        run: |
          SIGNATURE=$(cat signature.b64)
          cat > update.json << JSONEOF
          {
            "versionCode": ${{ steps.version.outputs.code }},
            "apkUrl": "https://github.com/${{ github.repository }}/releases/download/${{ env.RELEASE_TAG }}/${{ env.APK_NAME }}",
            "signature": "$SIGNATURE",
            "changelog": "Otomatik build - versiyon ${{ steps.version.outputs.code }}"
          }
          JSONEOF
          cat update.json

      - name: Release oluştur/güncelle
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          name: "OTA-Deneyi v${{ steps.version.outputs.code }}"
          make_latest: true
          files: |
            ${{ env.APK_NAME }}
            signature.b64
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: update.json'ı repoya commit'le
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add update.json
          git diff --cached --quiet && exit 0
          git commit -m "chore: update.json otomatik güncellendi (v${{ steps.version.outputs.code }})"
          git push

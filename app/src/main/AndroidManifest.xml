<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- =========================
         NETWORK
         ========================= -->

    <!-- OTA manifest / GitHub Release erişimi -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Ağ bağlantısı durumunu kontrol etmek için -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />


    <!-- =========================
         STORAGE
         ========================= -->

    <!-- Android 9 ve altı için geriye dönük uyumluluk -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- Android 12 ve altı eski storage senaryoları -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />


    <!-- =========================
         NOTIFICATIONS
         ========================= -->

    <!-- Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />


    <!-- =========================
         APK INSTALLATION
         ========================= -->

    <!-- OTA üzerinden indirilen APK'nın kurulum ekranını açabilmek için -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />


    <!-- =========================
         NFC / HIZLI AKTARIM
         ========================= -->

    <!-- NFC donanımı olmayan cihazlarda uygulama yine kurulabilsin -->
    <uses-feature
        android:name="android.hardware.nfc"
        android:required="false" />

    <!-- NFC erişimi -->
    <uses-permission android:name="android.permission.NFC" />


    <!-- =========================
         APPLICATION
         ========================= -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OtaDashboard"
        android:usesCleartextTraffic="false">

        <!-- =========================
             MAIN ACTIVITY
             ========================= -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.OtaDashboard">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>


        <!-- =========================
             FILE PROVIDER
             OTA APK INSTALLATION
             ========================= -->

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">

            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />

        </provider>

    </application>

</manifest>